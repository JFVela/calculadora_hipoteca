// src/theme.js
import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    primary: {
      main: '#FF8040', // Puedes cambiar esto por el color que desees, por ejemplo: '#007BFF'
    },
  },
});

export default customTheme;